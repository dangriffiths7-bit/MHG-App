import { defineConfig } from "vite";

// IMPORTANT: This must match your GitHub repo path.
// We are using 'my-pwa' exactly to keep it foolproof.
export default defineConfig({
  base: "/my-pwa/",
});
