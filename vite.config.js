import { defineConfig } from "vite";

// IMPORTANT: This must match your GitHub repo path.
// We are using 'MHG-App' exactly to keep it foolproof.
export default defineConfig({
  base: "/MHG-App/",
});
